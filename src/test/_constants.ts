import { PORT, TEST_FILE_SERVER_PORT } from '../_constants';

export const APP_URL = `http://localhost:${PORT}`;
export const APP_URL_WITH_API_KEY = `http://localhost:${PORT}?api_key=foo`;
export const FILE_URL = `http://localhost:${TEST_FILE_SERVER_PORT}`;
